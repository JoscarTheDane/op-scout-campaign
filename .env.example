# Copy to .env (or export in your shell) before running send_email.py
# AgentMail inbox-scoped key for the business inbox (send capability only).
# Create one at https://app.agentmail.to -> your inbox -> keys.
AGENTMAIL_KEY=am_us_your_key_here
